# .NET
bin/
obj/
*.user
*.suo
.vs/
appsettings.Development.json

# EF Core migrations — keep the Migrations folder but ignore generated snapshots if needed
# (leave migrations tracked so teammates can run dotnet ef database update)

# Node / React
library-frontend/node_modules/
library-frontend/dist/
library-frontend/.env.local
library-frontend/.env*.local

# OS
.DS_Store
Thumbs.db
